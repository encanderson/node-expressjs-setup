generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRESQL_URI")
}

model UserModel {
  id          Int     @id @default(autoincrement())
  name        String
  email       String  @unique
  password    String
  code        Int?
  createdAt   DateTime
  updatedAt   DateTime
}